{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["apps/**/.next/**", "packages/**/dist/**"]
    },
    "lint": {},
    "dev": {
      "cache": false
    }
  }
}
